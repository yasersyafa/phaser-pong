{
  "name": "pong-game-2d",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot ./index.html",
    "build": "bun build ./index.html --outdir=dist --minify --sourcemap=external",
    "preview": "bunx serve dist",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  }
}
